# Rule Engine

Pure deterministic rule calculation over structured `RuleRequest` objects. It has no LLM/Ollama dependency, never reads Adventures or natural-language player input, never writes GM Context, and knows no NPC names.

Supported modules in v0.1: ability check, skill check, saving throw, attack, damage, initiative, movement, interaction, inventory/use item, and no-roll direct resolution.
